COMPILER = gcc
CCFLAGS1 = -g
CCFLAGS2 = -lrt

clock:
	${COMPILER} ${CCFLAGS1} hw1_clock.c ${CCFLAGS2} -o hw1_clock

cache:
	${COMPILER} ${CCFLAGS1} cache.c ${CCFLAGS2} -o cache

clean:
	rm -rf hw1_clock cache